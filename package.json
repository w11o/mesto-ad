{
  "name": "mesto-ad",
  "version": "1.0.0",
  "description": "Mesto project — work with server",
  "main": "index.js",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "gh-pages": "^6.1.1"
  }
}
